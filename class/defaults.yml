parameters:
  cloudscale_cloud_controller_manager:
    =_metadata:
      multi_tenant: true
    namespace: syn-cloudscale-cloud-controller-manager

    # renovate: datasource=github-releases depName=cloudscale-ch/cloudscale-cloud-controller-manager
    manifests_version: 1.1.0

    args: []

    api_token: ?{vaultkv:${cluster:tenant}/${cluster:name}/cloudscale/token}
